# CPU 전용 PyTorch (CUDA 제외 - 용량 절약)
--extra-index-url https://download.pytorch.org/whl/cpu
torch

fastapi>=0.110
uvicorn>=0.24
pydantic>=2.0
pandas>=2.0
numpy>=1.24
joblib>=1.3
openai>=1.0
python-multipart>=0.0.6
openpyxl>=3.1
scikit-learn>=1.3        # ML 모델 (sklearn)
tqdm>=4.66               # 진행 표시줄

# Optional (RAG)
langchain>=0.2
langchain-openai>=0.1
langchain-community>=0.2
langchain-text-splitters>=0.2
langgraph>=0.2              # Multi-Agent 그래프
pypdf>=4.0
PyPDF2>=3.0
pymupdf>=1.24              # 강력한 PDF 파서 (fitz)

# Optional (추천 시스템 - LightFM 포함)
recommenders[lightfm]>=1.2.0

# MLOps
mlflow>=2.10

# OCR
easyocr>=1.7

# Advanced RAG (Hybrid Search + Reranking)
rank-bm25>=0.2.2          # BM25 키워드 검색
sentence-transformers>=2.2 # Cross-Encoder Reranking
faiss-cpu>=1.7            # 벡터 검색 (FAISS)

# ML Explainability
shap>=0.44                # SHAP explainer

# GraphRAG (LLM 기반 지식 그래프)
networkx>=3.0             # 그래프 라이브러리
lightrag-hku>=0.1.0       # LightRAG (Graph RAG)

# 최적화 (PSO 기반 팀 구성 최적화)
mealpy>=3.0               # 메타휴리스틱 최적화 (PPSO 포함)
xgboost>=2.0              # 팀 성능 예측 모델
lightgbm>=4.0             # 승률 예측 모델
